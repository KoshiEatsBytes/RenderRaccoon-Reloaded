Configure a single logged run: techniques, render distance, and biome scene.

Custom runs use the same camera path and logging as the deterministic benchmark, so they can be analyzed and compared like any other run, but are specifically marked as non-deterministic.

Useful for probing one specific pairing at range the deterministic benchmark does not include or to experiment.
